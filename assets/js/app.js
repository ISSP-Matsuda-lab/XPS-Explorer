    // Auger kinetic energies (eV). NIST SRD 20 recommended elemental
    // reference data are preferred; XDB principal Auger energies fill gaps.
    const augerLevels = {
      Be:[['KVV',102.50]],
      B:[['KVV',176.60]],
      C:[['KVV',266.80]],
      N:[['KLL',379]],
      O:[['KLL',510]],
      F:[['KLL',655]],
      Na:[['KLL',990]],
      Mg:[['KLL',1185.20]],
      Al:[['KLL',1393.09],['LVV',67.20]],
      Si:[['KLL',1615.50],['LVV',92.90]],
      P:[['KLL',1850]],
      S:[['KLL',2117]],
      Cl:[['KLL',2380]],
      K:[['LMM',252]],
      Ca:[['LMM',291]],
      Sc:[['LMM',337.10]],
      Ti:[['LMV',418.90]],
      V:[['LMV',471.80]],
      Cr:[['LMV',527.30]],
      Mn:[['LMV',586.20]],
      Fe:[['LVV',702.70]],
      Co:[['LVV',773.40],['MVV',53.60]],
      Ni:[['LVV',846.10],['MVV',59.70]],
      Cu:[['LMM',918.69],['MVV',62.37]],
      Zn:[['LMM',992.40],['MVV',59.90]],
      Ga:[['LMM',1067.90],['MVV',56.10]],
      Ge:[['LMM',1145.50]],
      As:[['LMM',1224.80]],
      Se:[['LMM',1306.70]],
      Y:[['LMM',1733.60],['MNV',130.60]],
      Zr:[['LMM',1837.10],['MNV',148.80]],
      Nb:[['LMM',1937.10],['MNV',167.70]],
      Mo:[['LMM',2037.10],['MNV',187.10]],
      Ru:[['MNN',274.30]],
      Rh:[['MNN',301.40]],
      Pd:[['MNN',327.40]],
      Ag:[['MNN',357.88]],
      Cd:[['MNN',383.60]],
      In:[['MNN',410.20]],
      Sn:[['MNN',437.20]],
      Sb:[['MNN',464.00]],
      Te:[['MNN',491.90]],
      I:[['MNN',511]],
      Nd:[['MNN',724.90]],
      Gd:[['MNN',883.50],['NVV',139.10]],
      Hf:[['MNN',1617.50],['NNN',170.00]],
      Ta:[['MNN',1674.80],['NNN',168.20]],
      W:[['MNN',1730.50],['NNN',166.50]],
      Re:[['MNN',1785.00],['NNN',163.90]],
      Os:[['NNN',175.10]],
      Ir:[['MNN',1900.80],['NNN',172.20]],
      Pt:[['MNN',1961.80],['NNN',169.70]],
      Au:[['MNN',2015.80],['NVV',71.23]],
      Pb:[['NOO',95.80]],
      Bi:[['MNN',2229.30],['NOO',103.60]]
    };
    const peaks = Object.fromEntries(Object.entries(coreLevels).map(([symbol, core]) => [symbol, {core, auger: augerLevels[symbol] || []}]));
    const symbols = ['H','He','Li','Be','B','C','N','O','F','Ne','Na','Mg','Al','Si','P','S','Cl','Ar','K','Ca','Sc','Ti','V','Cr','Mn','Fe','Co','Ni','Cu','Zn','Ga','Ge','As','Se','Br','Kr','Rb','Sr','Y','Zr','Nb','Mo','Tc','Ru','Rh','Pd','Ag','Cd','In','Sn','Sb','Te','I','Xe','Cs','Ba','La','Ce','Pr','Nd','Pm','Sm','Eu','Gd','Tb','Dy','Ho','Er','Tm','Yb','Lu','Hf','Ta','W','Re','Os','Ir','Pt','Au','Hg','Tl','Pb','Bi','Po','At','Rn','Fr','Ra','Ac','Th','Pa','U','Np','Pu','Am','Cm','Bk','Cf','Es','Fm','Md','No','Lr','Rf','Db','Sg','Bh','Hs','Mt','Ds','Rg','Cn','Nh','Fl','Mc','Lv','Ts','Og'];
    // Correct standard table coordinates explicitly for periods 4–7; lanthanides/actinides appear in separate rows.
    const coord = {};
    const rows=[['H',1],['He',18],['Li',1],['Be',2],['B',13],['C',14],['N',15],['O',16],['F',17],['Ne',18],['Na',1],['Mg',2],['Al',13],['Si',14],['P',15],['S',16],['Cl',17],['Ar',18]];
    rows.forEach(([s,c],i)=>coord[s]=[i<2?1:i<10?2:3,c]);
    ['K','Ca','Sc','Ti','V','Cr','Mn','Fe','Co','Ni','Cu','Zn','Ga','Ge','As','Se','Br','Kr'].forEach((s,i)=>coord[s]=[4,i+1]);
    ['Rb','Sr','Y','Zr','Nb','Mo','Tc','Ru','Rh','Pd','Ag','Cd','In','Sn','Sb','Te','I','Xe'].forEach((s,i)=>coord[s]=[5,i+1]);
    ['Cs','Ba','La','Hf','Ta','W','Re','Os','Ir','Pt','Au','Hg','Tl','Pb','Bi','Po','At','Rn'].forEach((s,i)=>coord[s]=[6,i+1]);
    ['Fr','Ra','Ac','Rf','Db','Sg','Bh','Hs','Mt','Ds','Rg','Cn','Nh','Fl','Mc','Lv','Ts','Og'].forEach((s,i)=>coord[s]=[7,i+1]);
    symbols.slice(57,71).forEach((s,i)=>coord[s]=[8,i+4]); symbols.slice(89,103).forEach((s,i)=>coord[s]=[9,i+4]);
    const SEARCH_WIDTH_MAX=10, STORAGE_DB='xps-peak-explorer', STORAGE_VERSION=1, STORAGE_STORE='workspace', STORAGE_KEY='current';
    const ANALYSIS_CODES={constant:'bg',height:'nh',area:'na'};
    const selected = new Map(), dataFiles = [], selectedSpectra = new Set(), analysisSnapshots = new Map(), plotOrder=[], layoutSelection=new Set(), layoutDirty=new Set(); let mode='binding', energyTableMode='selected', photon=1486.6, workFunction=4.5, includeHigherOrderExcitation=false, zoomDomain=null, zoomYDomain=null, plotScale=null, dragStart=null, dragSelection=null, offsetDrag=null, pendingAnalysis=null, activeAnalysis='raw', nextSpectrumId=0, analysisMessageTimer=null, storageReady=false, saveTimer=null, storageDatabase=null, plotLegendState={visible:false,x:.72,y:.12,entries:{}}, legendDrag=null, editingLegendId=null, focusedSpectrumId=null;
    const periodic=document.querySelector('#periodic'), selection=document.querySelector('#selection'), svg=document.querySelector('#plot'), tip=document.querySelector('#tooltip'), photonPreset=document.querySelector('#photonPreset'), photonEnergy=document.querySelector('#photonEnergy'), workFunctionInput=document.querySelector('#workFunction');
    const bindingButton=document.querySelector('#binding'), kineticButton=document.querySelector('#kinetic'), showCoreInput=document.querySelector('#showCore'), showAugerInput=document.querySelector('#showAuger'), axisLabelElement=document.querySelector('#axisLabel'), clearButton=document.querySelector('#clear');
    const cursorReadout=document.querySelector('#cursorReadout'), yAxisLabel=document.querySelector('#yAxisLabel'), energyList=document.querySelector('#energyList'), energyDetailsTitle=document.querySelector('#energyDetailsTitle'), energyDetailsHint=document.querySelector('#energyDetailsHint'), selectedEnergyMode=document.querySelector('#selectedEnergyMode'), searchEnergyMode=document.querySelector('#searchEnergyMode'), energySearchFields=document.querySelector('#energySearchFields'), peakEnergyInput=document.querySelector('#peakEnergy'), peakEnergyRange=document.querySelector('#peakEnergyRange'), energyToleranceInput=document.querySelector('#energyTolerance'), energyToleranceRange=document.querySelector('#energyToleranceRange'), firstOrderOnlyButton=document.querySelector('#firstOrderOnly'), includeHigherOrdersButton=document.querySelector('#includeHigherOrders'), energySearchSummary=document.querySelector('#energySearchSummary'), dataBrowser=document.querySelector('#dataBrowser'), xyFiles=document.querySelector('#xyFiles'), fileButton=document.querySelector('.file-button'), dragMenu=document.querySelector('#dragMenu'), analysisState=document.querySelector('#analysisState'), analysisMessage=document.querySelector('#analysisMessage'), analysisDialog=document.querySelector('#analysisDialog'), analysisForm=document.querySelector('#analysisForm'), analysisDialogTitle=document.querySelector('#analysisDialogTitle'), analysisEnergyMin=document.querySelector('#analysisEnergyMin'), analysisEnergyMax=document.querySelector('#analysisEnergyMax'), analysisDialogError=document.querySelector('#analysisDialogError'), resetAllButton=document.querySelector('#resetAll'), energyDetails=document.querySelector('#energyDetails'), dataPanel=document.querySelector('.data-panel');
    const plotLayoutButton=document.querySelector('#plotLayoutButton'), plotLayoutDialog=document.querySelector('#plotLayoutDialog'), plotLayoutForm=document.querySelector('#plotLayoutForm'), plotLayoutEmpty=document.querySelector('#plotLayoutEmpty'), plotLayoutBody=document.querySelector('#plotLayoutBody'), plotLayoutControls=document.querySelector('#plotLayoutControls'), plotSeriesList=document.querySelector('#plotSeriesList'), plotSelectionCount=document.querySelector('#plotSelectionCount'), plotSelectAll=document.querySelector('#plotSelectAll'), plotSelectNone=document.querySelector('#plotSelectNone'), plotRenderMode=document.querySelector('#plotRenderMode'), plotLineStyle=document.querySelector('#plotLineStyle'), plotLineWidth=document.querySelector('#plotLineWidth'), plotColor=document.querySelector('#plotColor'), plotColorPresets=document.querySelector('#plotColorPresets'), plotXOffset=document.querySelector('#plotXOffset'), plotYOffset=document.querySelector('#plotYOffset'), plotMarkerSize=document.querySelector('#plotMarkerSize'), plotMarkerStrokeWidth=document.querySelector('#plotMarkerStrokeWidth'), plotMarkerFilled=document.querySelector('#plotMarkerFilled'), plotMarkerShape=document.querySelector('#plotMarkerShape'), plotHidden=document.querySelector('#plotHidden'), plotStyleReset=document.querySelector('#plotStyleReset');
    const plotOffsetsButton=document.querySelector('#plotOffsetsButton'), plotOffsetsDialog=document.querySelector('#plotOffsetsDialog'), plotOffsetsForm=document.querySelector('#plotOffsetsForm'), plotXOffsetStep=document.querySelector('#plotXOffsetStep'), plotYOffsetStep=document.querySelector('#plotYOffsetStep'), plotOffsetsError=document.querySelector('#plotOffsetsError'), plotLegendButton=document.querySelector('#plotLegendButton'), plotLegend=document.querySelector('#plotLegend'), legendEditDialog=document.querySelector('#legendEditDialog'), legendEditForm=document.querySelector('#legendEditForm'), legendItemName=document.querySelector('#legendItemName'), legendItemFontSize=document.querySelector('#legendItemFontSize'), deleteLegendItem=document.querySelector('#deleteLegendItem');
    const fitDialog=document.querySelector('#fitDialog'), fitForm=document.querySelector('#fitForm'), fitSpectrumName=document.querySelector('#fitSpectrumName'), fitMethod=document.querySelector('#fitMethod'), fitEnergyMin=document.querySelector('#fitEnergyMin'), fitEnergyMax=document.querySelector('#fitEnergyMax'), fitPeakCount=document.querySelector('#fitPeakCount'), fitPeakCountLabel=document.querySelector('#fitPeakCountLabel'), fitBackground=document.querySelector('#fitBackground'), fitBackgroundLabel=document.querySelector('#fitBackgroundLabel'), fitPeakSection=document.querySelector('#fitPeakSection'), fitPeakRows=document.querySelector('#fitPeakRows'), fitNote=document.querySelector('#fitNote'), fitDialogError=document.querySelector('#fitDialogError');
    const fitStyleDialog=document.querySelector('#fitStyleDialog'), fitStyleForm=document.querySelector('#fitStyleForm'), fitStyleName=document.querySelector('#fitStyleName'), fitLineColor=document.querySelector('#fitLineColor'), fitLineStyle=document.querySelector('#fitLineStyle'), fitLineWidth=document.querySelector('#fitLineWidth'), fitShowComponents=document.querySelector('#fitShowComponents'), fitShowComponentsLabel=document.querySelector('#fitShowComponentsLabel'), fitShowBackground=document.querySelector('#fitShowBackground'), fitShowBackgroundLabel=document.querySelector('#fitShowBackgroundLabel'), fitShowAnnotation=document.querySelector('#fitShowAnnotation'), fitShowAnnotationLabel=document.querySelector('#fitShowAnnotationLabel');
    function openStorage(){
      if(storageDatabase)return Promise.resolve(storageDatabase);
      return new Promise((resolve,reject)=>{const request=indexedDB.open(STORAGE_DB,STORAGE_VERSION);request.onupgradeneeded=()=>request.result.createObjectStore(STORAGE_STORE);request.onsuccess=()=>{storageDatabase=request.result;resolve(storageDatabase)};request.onerror=()=>reject(request.error)});
    }
    async function readStoredWorkspace(){const db=await openStorage();return new Promise((resolve,reject)=>{const request=db.transaction(STORAGE_STORE,'readonly').objectStore(STORAGE_STORE).get(STORAGE_KEY);request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(request.error)})}
    function workspaceState(){return{
      version:2,selected:[...selected],dataFiles,selectedSpectra:[...selectedSpectra],analysisSnapshots:[...analysisSnapshots].map(([id,snapshot])=>[id,{...snapshot,points:[...snapshot.points]}]),activeAnalysis,nextSpectrumId,
      ui:{mode,energyTableMode,photon,workFunction,includeHigherOrderExcitation,zoomDomain,zoomYDomain,plotOrder:[...plotOrder],plotLegend:plotLegendState,showCore:showCoreInput.checked,showAuger:showAugerInput.checked,peakEnergy:peakEnergyInput.value,energyTolerance:energyToleranceInput.value,energyDetailsOpen:energyDetails.open,dataPanelOpen:dataPanel.open}
    }}
    async function persistWorkspace(){
      if(!storageReady)return;
      try{const db=await openStorage(),state=workspaceState();await new Promise((resolve,reject)=>{const request=db.transaction(STORAGE_STORE,'readwrite').objectStore(STORAGE_STORE).put(state,STORAGE_KEY);request.onsuccess=()=>resolve();request.onerror=()=>reject(request.error)})}catch(error){console.error('Could not save workspace state',error)}
    }
    function schedulePersist(){if(!storageReady)return;clearTimeout(saveTimer);saveTimer=setTimeout(persistWorkspace,150)}
    function restoreWorkspace(state){
      if(!state||![1,2].includes(state.version))return false;
      selected.clear();(state.selected||[]).forEach(([symbol,item])=>{if(peaks[symbol])selected.set(symbol,item)});
      const restoredFiles=(state.dataFiles||[]).filter(file=>state.version===2||!String(file.name||'').startsWith('data/'));dataFiles.splice(0,dataFiles.length,...restoredFiles);dataFiles.forEach(file=>file.spectra.forEach(s=>{s.results=s.results||[];s.fits=s.fits||[]}));selectedSpectra.clear();(state.selectedSpectra||[]).forEach(id=>selectedSpectra.add(id));
      analysisSnapshots.clear();(state.analysisSnapshots||[]).forEach(([id,snapshot])=>analysisSnapshots.set(id,{...snapshot,points:new Map(snapshot.points||[])}));
      activeAnalysis=state.activeAnalysis==='raw'||analysisSnapshots.has(state.activeAnalysis)?state.activeAnalysis:'raw';nextSpectrumId=Number.isInteger(state.nextSpectrumId)?state.nextSpectrumId:0;
      const ui=state.ui||{};mode=ui.mode==='kinetic'?'kinetic':'binding';energyTableMode=ui.energyTableMode==='search'?'search':'selected';photon=Number.isFinite(ui.photon)&&ui.photon>0?ui.photon:1486.6;workFunction=Number.isFinite(ui.workFunction)&&ui.workFunction>=0?ui.workFunction:4.5;includeHigherOrderExcitation=Boolean(ui.includeHigherOrderExcitation);zoomDomain=Array.isArray(ui.zoomDomain)?ui.zoomDomain:null;zoomYDomain=Array.isArray(ui.zoomYDomain)?ui.zoomYDomain:null;plotOrder.splice(0,plotOrder.length,...(Array.isArray(ui.plotOrder)?ui.plotOrder:[]));normalizePlotOrder();plotLegendState=normalizeLegendState(ui.plotLegend);
      showCoreInput.checked=ui.showCore!==false;showAugerInput.checked=Boolean(ui.showAuger);photonEnergy.value=String(photon);const preset=[...photonPreset.options].find(option=>option.value!=='custom'&&Number(option.value)===photon);photonPreset.value=preset?preset.value:'custom';workFunctionInput.value=String(workFunction);peakEnergyInput.value=ui.peakEnergy??'285.0';energyToleranceInput.value=ui.energyTolerance??'0.5';energyDetails.open=ui.energyDetailsOpen!==false;dataPanel.open=ui.dataPanelOpen===true;
      document.querySelectorAll('.element').forEach(button=>button.classList.toggle('selected',selected.has(button.querySelector('.symbol').textContent)));bindingButton.classList.toggle('active',mode==='binding');kineticButton.classList.toggle('active',mode==='kinetic');axisLabelElement.textContent=mode==='binding'?'Binding energy (eV) — high to low':'Kinetic energy (eV) — low to high';firstOrderOnlyButton.classList.toggle('active',!includeHigherOrderExcitation);includeHigherOrdersButton.classList.toggle('active',includeHigherOrderExcitation);
      return true;
    }
    symbols.forEach((s,i)=>{ const [r,c]=coord[s]||[9,18]; const b=document.createElement('button'); b.className='element'+(peaks[s]?'':' no-data'); b.style.gridRow=r; b.style.gridColumn=c; b.innerHTML=`<span class="z">${i+1}</span><span class="symbol">${s}</span>`; b.disabled=!peaks[s]; b.title=peaks[s]?`${s}: ${peaks[s].core.length} core levels`:`${s}: no binding-energy data`; b.onclick=()=>toggle(s,b); periodic.append(b); });
    function elementColor(s){ return `hsl(${(symbols.indexOf(s)*137.508)%360} 68% 65%)`; }
    function toggle(s,b){ if(selected.has(s)){selected.delete(s);b.classList.remove('selected')} else {selected.set(s,{color:elementColor(s),visible:true});b.classList.add('selected')} render(); }
    function render(){ renderSelection(); renderEnergyDetails(); renderPlot(); }
    function renderSelection(){
      selection.innerHTML=selected.size?'':'<div class="empty">Select an element in the periodic table<br>to display its peak lines</div>';
      selected.forEach((item,s)=>{ const d=document.createElement('div'); d.className='chip'+(item.visible?'':' off'); d.setAttribute('role','button'); d.tabIndex=0; d.setAttribute('aria-pressed',String(item.visible)); d.title=`${item.visible?'Hide':'Show'} ${s} in the plot`; d.innerHTML=`<i class="swatch" style="background:${item.color}"></i><div><strong>${s}</strong><small>${item.visible?'Visible':'Hidden'} · ${peaks[s].core.length} core · ${peaks[s].auger.length} Auger</small></div><button class="remove" aria-label="Remove ${s} from selection" title="Remove from selection">×</button>`;
        const switchVisibility=()=>{item.visible=!item.visible;render()}; d.onclick=switchVisibility; d.onkeydown=e=>{if(!e.target.closest('.remove')&&(e.key==='Enter'||e.key===' ')){e.preventDefault();switchVisibility()}}; d.querySelector('.remove').onclick=e=>{e.stopPropagation();toggle(s,elementButton(s))}; selection.append(d);
      });
    }
    function elementButton(s){return [...periodic.children].find(x=>x.querySelector('.symbol').textContent===s)}
    function displayedEnergy(raw,type){return (mode==='kinetic'&&type==='core')||(mode==='binding'&&type==='auger')?photon-raw-workFunction:raw}
    function peakTypeLabel(){
      const labels=[];
      if(showCoreInput.checked)labels.push('Core');
      if(showAugerInput.checked)labels.push('Auger');
      return labels.length?labels.join(' + '):'None';
    }
    function searchedCoreEnergy(bindingEnergy,order){
      const excitation=photon*order;
      if(bindingEnergy+workFunction>excitation)return null;
      const kineticEnergy=excitation-bindingEnergy-workFunction;
      return mode==='kinetic'?kineticEnergy:photon-kineticEnergy-workFunction;
    }
    function orbitalOrder(label){const match=label.match(/^(\d+)([spdf])(\d+\/\d+)?$/i);if(!match)return [99,label];const subshell={s:0,p:1,d:2,f:3}[match[2].toLowerCase()];const spin=match[3]||'';return [Number(match[1])*10+subshell,spin]}
    function compareOrbitals(a,b){const aa=orbitalOrder(a),bb=orbitalOrder(b);return aa[0]-bb[0]||aa[1].localeCompare(bb[1],undefined,{numeric:true})}
    function renderEnergyDetails(){
      const modeLabel=mode==='binding'?'Binding energy':'Kinetic energy'; energyDetailsTitle.textContent=`${modeLabel} values`; energyDetailsHint.textContent=`${peakTypeLabel()} · ${photon.toLocaleString()} eV source · φ ${workFunction.toLocaleString()} eV`;
      energyList.innerHTML='';
      energySearchFields.hidden=energyTableMode!=='search';
      selectedEnergyMode.classList.toggle('active',energyTableMode==='selected');searchEnergyMode.classList.toggle('active',energyTableMode==='search');
      if(energyTableMode==='search'){renderEnergySearch(modeLabel);return}
      energySearchSummary.textContent='';
      if(!selected.size){energyList.innerHTML='<div class="empty">Select an element to display its energy values</div>';return}
      const rows=[];selected.forEach((item,s)=>{if(showCoreInput.checked)rows.push({item,s,kind:'Core',type:'core',values:peaks[s].core});if(showAugerInput.checked&&peaks[s].auger.length)rows.push({item,s,kind:'Auger',type:'auger',values:peaks[s].auger})});
      if(!rows.length){energyList.innerHTML='<div class="empty">Enable Core or Auger peaks</div>';return}
      const labels=[...new Set(rows.flatMap(row=>row.values.map(([label])=>label)))].sort(compareOrbitals);
      const table=document.createElement('table');table.className='energy-table';
      table.innerHTML=`<thead><tr><th scope="col">Element</th><th scope="col">Type</th>${labels.map(label=>`<th scope="col">${label}</th>`).join('')}</tr></thead>`;
      const body=document.createElement('tbody');rows.forEach(({item,s,kind,type,values})=>{const valuesByLabel=new Map(values),atomicNumber=symbols.indexOf(s)+1;const row=document.createElement('tr');row.className=item.visible?'':'is-hidden';row.innerHTML=`<th scope="row" style="color:${item.color}">${s}<sup class="atomic-number">${atomicNumber}</sup></th><td>${kind}</td>${labels.map(label=>{if(!valuesByLabel.has(label))return '<td>—</td>';const raw=valuesByLabel.get(label),energy=displayedEnergy(raw,type),overPhoton=energy>photon;return `<td><span class="energy-value${overPhoton?' is-over-photon':''}">${energy>=0?energy.toFixed(1):'—'} eV</span></td>`}).join('')}`;body.append(row)});table.append(body);energyList.append(table);
      const legend=document.createElement('p');legend.className='energy-legend';legend.textContent=`● Red: ${modeLabel} exceeds the excitation photon energy`;energyList.append(legend);
    }
    function renderEnergySearch(modeLabel){
      const target=peakEnergyInput.valueAsNumber,tolerance=normalizedSearchWidth();
      if(!Number.isFinite(target)||target<0||!Number.isFinite(tolerance)){energySearchSummary.textContent='';energyList.innerHTML='<div class="empty">Enter a non-negative peak energy and search width</div>';return}
      const matches=[];
      Object.entries(peaks).forEach(([symbol,data])=>{
        if(showCoreInput.checked)data.core.forEach(([level,raw])=>{(includeHigherOrderExcitation?[1,2,3]:[1]).forEach(order=>{const energy=searchedCoreEnergy(raw,order);if(energy!=null&&energy>=0&&Math.abs(energy-target)<=tolerance)matches.push({symbol,kind:'Core',level,order,energy,difference:energy-target})})});
        if(showAugerInput.checked)data.auger.forEach(([level,raw])=>{const energy=displayedEnergy(raw,'auger');if(energy>=0&&Math.abs(energy-target)<=tolerance)matches.push({symbol,kind:'Auger',level,order:null,energy,difference:energy-target})});
      });
      matches.sort((a,b)=>Math.abs(a.difference)-Math.abs(b.difference)||a.energy-b.energy||(a.order||0)-(b.order||0)||symbols.indexOf(a.symbol)-symbols.indexOf(b.symbol));
      energySearchSummary.textContent=`${target.toFixed(1)} ± ${tolerance.toFixed(1)} eV · ${matches.length.toLocaleString()} matches`;
      if(!showCoreInput.checked&&!showAugerInput.checked){energyList.innerHTML='<div class="empty">Enable Core or Auger peaks</div>';return}
      if(!matches.length){energyList.innerHTML='<div class="empty">No elements match the specified range</div>';return}
      const table=document.createElement('table');table.className='energy-table energy-search-table';
      table.innerHTML='<thead><tr><th scope="col">Element</th><th scope="col">Type</th><th scope="col">Level / transition</th><th scope="col">Excitation</th><th scope="col">Energy</th><th scope="col">Difference</th></tr></thead>';
      const body=document.createElement('tbody');matches.forEach(({symbol,kind,level,order,energy,difference})=>{const row=document.createElement('tr'),atomicNumber=symbols.indexOf(symbol)+1;const excitation=order?`Order ${order} (${(photon*order).toFixed(1)} eV)`:'—';row.innerHTML=`<th scope="row" style="color:${elementColor(symbol)}">${symbol}<sup class="atomic-number">${atomicNumber}</sup></th><td>${kind}</td><td>${level}</td><td>${excitation}</td><td><span class="energy-value">${energy.toFixed(1)} eV</span></td><td>${difference>=0?'+':''}${difference.toFixed(1)} eV</td>`;body.append(row)});table.append(body);energyList.append(table);
      const note=document.createElement('p');note.className='energy-legend';note.textContent=`Searching ${modeLabel.toLowerCase()} · Core uses ${includeHigherOrderExcitation?'first- to third-order':'first-order'} excitation; Auger uses order-independent kinetic energy`;energyList.append(note);
    }
    function axisScale(value){ const rough=Math.max(value/8,1e-9), power=10**Math.floor(Math.log10(rough)), fraction=rough/power, step=(fraction<=1?1:fraction<=2?2:fraction<=5?5:10)*power; return {step,max:Math.ceil(value/step)*step}; }
    function dataColor(index){const colors=['#62c9f5','#d88cff','#ff8e77','#7de0a3','#ffd166','#8aa8ff','#f47fb3','#64d8cb'];return colors[index%colors.length]}
    function defaultPlotStyle(){return{renderMode:'line',lineStyle:'solid',lineWidth:1.5,xOffset:0,yOffset:0,markerSize:3.5,markerStrokeWidth:1,markerFilled:true,markerShape:'circle',hidden:false}}
    function plotStyle(s){const raw=s.plotStyle||{},defaults=defaultPlotStyle();return{renderMode:['line','line-markers','markers'].includes(raw.renderMode)?raw.renderMode:defaults.renderMode,lineStyle:raw.lineStyle==='dashed'?'dashed':'solid',lineWidth:Number.isFinite(Number(raw.lineWidth))?Math.min(12,Math.max(.25,Number(raw.lineWidth))):defaults.lineWidth,xOffset:Number.isFinite(Number(raw.xOffset))?Number(raw.xOffset):0,yOffset:Number.isFinite(Number(raw.yOffset))?Number(raw.yOffset):0,markerSize:Number.isFinite(Number(raw.markerSize))?Math.min(20,Math.max(1,Number(raw.markerSize))):defaults.markerSize,markerStrokeWidth:Number.isFinite(Number(raw.markerStrokeWidth))?Math.min(10,Math.max(0,Number(raw.markerStrokeWidth))):defaults.markerStrokeWidth,markerFilled:raw.markerFilled!==false,markerShape:['circle','square','diamond','triangle','cross'].includes(raw.markerShape)?raw.markerShape:defaults.markerShape,hidden:Boolean(raw.hidden)}}
    function parseXY(text){return XYParser.parseXY(text)}
    function addDataFile(name,text){const spectra=parseXY(text);if(!spectra.length)throw new Error('No Region with energy / counts/s data was found');spectra.forEach(s=>{const index=nextSpectrumId++;s.id=`data-${index}`;s.color=dataColor(index);s.plotStyle=defaultPlotStyle();s.fits=s.fits||[];(s.results||[]).forEach(operation=>operation.curves.forEach(curve=>{curve.visible=Boolean(curve.visible)}));plotOrder.push(s.id)});dataFiles.push({name,spectra});renderDataBrowser();renderFitResults();renderPlot()}
    function removeDataFile(file){file.spectra.forEach(s=>{selectedSpectra.delete(s.id);layoutSelection.delete(s.id);const orderIndex=plotOrder.indexOf(s.id);if(orderIndex>=0)plotOrder.splice(orderIndex,1);analysisSnapshots.forEach(snapshot=>snapshot.points.delete(s.id))});const index=dataFiles.indexOf(file);if(index>=0)dataFiles.splice(index,1);zoomDomain=null;zoomYDomain=null;renderDataBrowser();renderFitResults();renderPlot()}
    function spectrumResultCount(s){return(s.fits||[]).length+(s.results||[]).reduce((sum,operation)=>sum+operation.curves.filter(curve=>!curve.removed).length,0)}
    function setGroupSelection(spectra,selected){spectra.forEach(s=>{s.groupPlotHidden=false;selected?selectedSpectra.add(s.id):selectedSpectra.delete(s.id);(s.fits||[]).forEach(fit=>{fit.visible=selected});(s.results||[]).forEach(operation=>operation.curves.filter(curve=>!curve.removed).forEach(curve=>{curve.visible=selected}))});zoomDomain=null;zoomYDomain=null;renderDataBrowser();renderPlot()}
    function renderDataBrowser(){
      dataBrowser.innerHTML='';
      if(!dataFiles.length){dataBrowser.innerHTML='<span class="hint">No XY data has been loaded. Use Add XY files to import spectra.</span>';return}
      dataFiles.forEach(file=>{
        const row=document.createElement('details');row.className='data-file';row.open=true;
        const name=document.createElement('summary');name.className='data-file-name';
        const nameText=document.createElement('span');nameText.className='file-name-text';nameText.textContent=`${file.name} (${file.spectra.length.toLocaleString()} spectra)`;
        const remove=document.createElement('button');remove.type='button';remove.className='delete-data';remove.textContent='Delete';remove.setAttribute('aria-label',`Delete ${file.name}`);remove.onclick=event=>{event.preventDefault();event.stopPropagation();removeDataFile(file)};name.append(nameText,remove);
        const groups=document.createElement('div');groups.className='data-groups';const byGroup=new Map();file.spectra.forEach(s=>{const key=s.group||'Ungrouped';if(!byGroup.has(key))byGroup.set(key,[]);byGroup.get(key).push(s)});
        byGroup.forEach((spectra,groupName)=>{
          const groupWrap=document.createElement('div');groupWrap.className='data-group-wrap';const group=document.createElement('details');group.className='data-group';group.open=spectra[0]?.groupOpen!==false;group.ontoggle=()=>{spectra.forEach(s=>{s.groupOpen=group.open});schedulePersist()};const groupLabel=document.createElement('summary');groupLabel.className='data-group-name';groupLabel.textContent=`${groupName} (${spectra.length})`;const groupActions=document.createElement('div');groupActions.className='group-plot-actions';const selectAll=document.createElement('button');selectAll.type='button';selectAll.className='group-plot-toggle';selectAll.textContent='Select all';selectAll.title='Select and plot every measurement and fitting result in this group';selectAll.onclick=()=>setGroupSelection(spectra,true);const clearAll=document.createElement('button');clearAll.type='button';clearAll.className='group-plot-toggle';clearAll.textContent='Clear all';clearAll.title='Clear every measurement and fitting result in this group';clearAll.onclick=()=>setGroupSelection(spectra,false);groupActions.append(selectAll,clearAll);const series=document.createElement('div');series.className='data-series';
          spectra.forEach(s=>{
            const entry=document.createElement('div');entry.className='series-entry';entry.dataset.spectrumEntry=s.id;entry.classList.toggle('is-graph-selected',s.id===focusedSpectrumId);
            const spectrumRow=document.createElement('div');spectrumRow.className='series-row';
            const label=document.createElement('label');label.className='series-option';label.style.setProperty('--series-color',s.color);
            const input=document.createElement('input');input.type='checkbox';input.dataset.spectrumId=s.id;input.checked=selectedSpectra.has(s.id);input.onchange=()=>{if(input.checked){selectedSpectra.add(s.id);s.groupPlotHidden=false}else selectedSpectra.delete(s.id);zoomDomain=null;zoomYDomain=null;renderDataBrowser();renderPlot()};
            const text=document.createElement('span');text.textContent=s.region;const meta=document.createElement('small'),style=plotStyle(s),offsetText=Math.abs(style.xOffset)>1e-12||Math.abs(style.yOffset)>1e-12?` · Δx ${style.xOffset.toFixed(3)} eV · Δy ${formatIntensity(style.yOffset)}`:'';meta.textContent=`ID ${s.spectrumId||'—'} · C${s.cycle||'0'}/${s.curve||'0'} · ${s.points.length.toLocaleString()} points${offsetText}`;label.title=`${s.group} / ${s.region} / Spectrum ${s.spectrumId||'—'}`;label.append(input,text,meta);spectrumRow.append(label);
            const rowActions=document.createElement('div');rowActions.className='series-row-actions';const addFit=document.createElement('button');addFit.type='button';addFit.className='add-fit';addFit.textContent='+';addFit.title='Add fitting result';addFit.setAttribute('aria-label',`Add fitting result for ${s.region}`);addFit.onclick=()=>openFitDialog(s);rowActions.append(addFit);spectrumRow.append(rowActions);entry.append(spectrumRow);
            const resultCount=spectrumResultCount(s);if(resultCount){const fitting=document.createElement('details');fitting.className='series-fitting';fitting.open=s.fitResultsOpen!==false;fitting.ontoggle=()=>{s.fitResultsOpen=fitting.open;schedulePersist()};const fitHead=document.createElement('summary');fitHead.className='series-fitting-head';const fitLabel=document.createElement('strong');fitLabel.textContent='Fitting results';const fitCount=document.createElement('span');fitCount.className='hint';fitCount.dataset.fitCount=s.id;fitHead.append(fitLabel,fitCount);const results=document.createElement('div');results.className='series-fit-results';results.dataset.fitResults=s.id;fitting.append(fitHead,results);entry.append(fitting)}series.append(entry)
          });
          group.append(groupLabel,series);groupWrap.append(group,groupActions);groups.append(groupWrap)
        });
        row.append(name,groups);dataBrowser.append(row)
      });
      renderFitResults();
    }
    function allSpectra(){return dataFiles.flatMap(file=>file.spectra)}
    function normalizePlotOrder(){const ids=allSpectra().map(s=>s.id),valid=new Set(ids),normalized=plotOrder.filter((id,index)=>valid.has(id)&&plotOrder.indexOf(id)===index);ids.forEach(id=>{if(!normalized.includes(id))normalized.push(id)});plotOrder.splice(0,plotOrder.length,...normalized)}
    function normalizeLegendState(raw){const entries={};Object.entries(raw?.entries||{}).forEach(([id,item])=>{entries[id]={name:typeof item.name==='string'?item.name:'',fontSize:Math.min(48,Math.max(8,Number(item.fontSize)||12)),deleted:Boolean(item.deleted)}});const x=Number(raw?.x),y=Number(raw?.y);return{visible:Boolean(raw?.visible),x:Number.isFinite(x)?Math.min(1,Math.max(0,x)):.72,y:Number.isFinite(y)?Math.min(1,Math.max(0,y)):.12,entries}}
    function selectedData(){normalizePlotOrder();const byId=new Map(allSpectra().map(s=>[s.id,s]));return plotOrder.map(id=>byId.get(id)).filter(s=>s&&selectedSpectra.has(s.id))}
    function fitTypeLabel(type){return{'gaussian':'Gaussian','lorentzian':'Lorentzian','voigt':'Voigt','doniach-sunjic':'Doniach–Šunjić','fermi-edge':'Fermi edge'}[type]||type}
    function fitColor(index){return['#ffdc7a','#80f0d0','#ff91b8','#b99aff','#ff9c73','#8ed0ff'][index%6]}
    function backgroundTypeLabel(type){return{none:'No background',constant:'Constant background',linear:'Linear background',shirley:'Shirley background'}[type]||'Linear background'}
    function normalizeFitLineStyle(item,imported=false){item.color=item.color||'#ffdc7a';item.lineStyle=['solid','dashed','dotted'].includes(item.lineStyle)?item.lineStyle:(imported?'dashed':'solid');item.lineWidth=Number.isFinite(Number(item.lineWidth))?Math.min(12,Math.max(.25,Number(item.lineWidth))):(imported?2:2.5);if(item.showComponents==null)item.showComponents=true;if(item.showBackground==null)item.showBackground=true;if(item.showAnnotation==null)item.showAnnotation=true;return item}
    let fitTargetSpectrum=null,fitStyleTarget=null;
    function openFitStyle(item,name,supportsComponents,supportsBackground,supportsAnnotation=false,imported=false){normalizeFitLineStyle(item,imported);fitStyleTarget=item;fitStyleName.textContent=name;fitLineColor.value=item.color;fitLineStyle.value=item.lineStyle;fitLineWidth.value=String(item.lineWidth);fitShowComponents.checked=item.showComponents;fitShowBackground.checked=item.showBackground;fitShowAnnotation.checked=item.showAnnotation;fitShowComponentsLabel.hidden=!supportsComponents;fitShowBackgroundLabel.hidden=!supportsBackground;fitShowAnnotationLabel.hidden=!supportsAnnotation;fitStyleDialog.showModal();fitLineColor.focus()}
    function renderFitResults(){
      allSpectra().forEach(s=>{
        const container=dataBrowser.querySelector(`[data-fit-results="${s.id}"]`),counter=dataBrowser.querySelector(`[data-fit-count="${s.id}"]`);if(!container)return;container.innerHTML='';let count=0;
        (s.fits||[]).forEach(fit=>{count++;normalizeFitLineStyle(fit);const card=document.createElement('div');card.className='fit-result-card';card.style.setProperty('--fit-color',fit.color);const title=document.createElement('label');title.className='fit-result-title';const toggle=document.createElement('input');toggle.type='checkbox';toggle.checked=fit.visible!==false;toggle.onchange=()=>{fit.visible=toggle.checked;if(toggle.checked){selectedSpectra.add(s.id);s.groupPlotHidden=false}renderDataBrowser();renderPlot()};const swatch=document.createElement('i');const text=document.createElement('span');text.textContent=fit.name;title.append(toggle,swatch,text);const actions=document.createElement('div');actions.className='fit-result-actions';const style=document.createElement('button');style.type='button';style.textContent='Style';style.onclick=()=>openFitStyle(fit,`${s.region} · ${fit.name}`,Boolean(fit.componentPoints?.length),Boolean(fit.backgroundPoints?.length),fit.type==='fermi-edge');const remove=document.createElement('button');remove.type='button';remove.textContent='−';remove.title='Delete fitting result';remove.onclick=()=>{s.fits.splice(s.fits.indexOf(fit),1);renderDataBrowser();renderPlot()};actions.append(style,remove);const meta=document.createElement('div');meta.className='fit-result-meta';const peakText=fit.parameters.peaks?fit.parameters.peaks.map((peak,index)=>`P${index+1}: ${peak.center.toFixed(3)} eV, FWHM ${peak.width.toFixed(3)} eV`).join(' · '):`Center ${fit.parameters.center.toFixed(3)} eV · FWHM ${fit.parameters.width.toFixed(3)} eV`,background=fit.type==='fermi-edge'?(fit.parameters.backgroundSide==='higher'?'Linear above EF':'Linear below EF'):backgroundTypeLabel(fit.parameters.backgroundType);meta.textContent=`${fitTypeLabel(fit.type)} · ${background} · R² ${fit.rSquared.toFixed(5)} · RMS ${formatIntensity(fit.rms)} · ${peakText}`;card.append(title,actions,meta);container.append(card)});
        (s.results||[]).forEach((operation,operationIndex)=>operation.curves.filter(curve=>!curve.removed).forEach((curve,curveIndex)=>{count++;curve.color=curve.color||fitColor(operationIndex+curveIndex+2);normalizeFitLineStyle(curve,true);const card=document.createElement('div');card.className='fit-result-card';card.style.setProperty('--fit-color',curve.color);const title=document.createElement('label');title.className='fit-result-title';const toggle=document.createElement('input');toggle.type='checkbox';toggle.checked=Boolean(curve.visible);toggle.onchange=()=>{curve.visible=toggle.checked;if(toggle.checked){selectedSpectra.add(s.id);s.groupPlotHidden=false}renderDataBrowser();renderPlot()};const swatch=document.createElement('i');const text=document.createElement('span');text.textContent=`${operation.name} · ${curve.name}`;title.append(toggle,swatch,text);const actions=document.createElement('div');actions.className='fit-result-actions';const style=document.createElement('button');style.type='button';style.textContent='Style';const isFermi=operation.name.toLowerCase().includes('fermi')&&curve.name.toLowerCase().includes('fermi');style.onclick=()=>openFitStyle(curve,`${s.region} · ${operation.name} · ${curve.name}`,false,false,isFermi,true);const remove=document.createElement('button');remove.type='button';remove.textContent='−';remove.title='Remove fitting result';remove.onclick=()=>{curve.removed=true;curve.visible=false;renderDataBrowser();renderPlot()};actions.append(style,remove);const meta=document.createElement('div');meta.className='fit-result-meta';const range=operation.range&&operation.range.every(Number.isFinite)?` · source range ${operation.range[0]}–${operation.range[1]} eV`:'';meta.textContent=`Imported result · ${curve.points.length.toLocaleString()} points${range}`;card.append(title,actions,meta);container.append(card)}));
        if(counter)counter.textContent=`${count} result${count===1?'':'s'}`
      })
    }
    function spectrumFitPoints(s,min,max){return displayedPoints(s).map(([kinetic,intensity])=>[spectrumEnergy(s,kinetic),intensity]).filter(([energy,intensity])=>energy>=min&&energy<=max&&Number.isFinite(intensity)).sort((a,b)=>a[0]-b[0])}
    function initialPeakSeeds(points,count){
      const sorted=points.slice().sort((a,b)=>a[0]-b[0]),minimum=Math.min(...sorted.map(point=>point[1])),span=sorted[sorted.length-1][0]-sorted[0][0],candidates=[];
      for(let i=1;i<sorted.length-1;i++)if(sorted[i][1]>=sorted[i-1][1]&&sorted[i][1]>=sorted[i+1][1])candidates.push(sorted[i]);
      candidates.sort((a,b)=>b[1]-a[1]);const chosen=[];const separation=span/Math.max(count*4,8);
      candidates.forEach(point=>{if(chosen.length<count&&chosen.every(other=>Math.abs(other[0]-point[0])>=separation))chosen.push(point)});
      while(chosen.length<count){const index=chosen.length;const center=sorted[0][0]+span*(index+1)/(count+1),nearest=sorted.reduce((best,point)=>Math.abs(point[0]-center)<Math.abs(best[0]-center)?point:best,sorted[0]);chosen.push(nearest)}
      return chosen.sort((a,b)=>a[0]-b[0]).map(point=>({center:point[0],height:Math.max(point[1]-minimum,1e-9),width:Math.max(span/(count*6),span/1000),shape:fitMethod.value==='voigt'?.5:.08}));
    }
    function renderFitPeakRows(){
      const count=Math.max(1,Math.min(8,Number(fitPeakCount.value)||1)),old=[...fitPeakRows.querySelectorAll('.fit-peak-row')].map(row=>({center:Number(row.querySelector('[data-field="center"]').value),height:Number(row.querySelector('[data-field="height"]').value),width:Number(row.querySelector('[data-field="width"]').value),shape:Number(row.querySelector('[data-field="shape"]').value)}));const spectrum=fitTargetSpectrum,min=fitEnergyMin.valueAsNumber,max=fitEnergyMax.valueAsNumber,points=spectrum&&Number.isFinite(min)&&Number.isFinite(max)?spectrumFitPoints(spectrum,min,max):[],seeds=points.length?initialPeakSeeds(points,count):[];fitPeakRows.innerHTML='';
      for(let index=0;index<count;index++){const seed=old[index]||seeds[index]||{center:0,height:1,width:1,shape:.5},row=document.createElement('div');row.className='fit-peak-row';const shapeLabel=fitMethod.value==='doniach-sunjic'?'Asymmetry α':'Lorentz fraction';row.innerHTML=`<span class="fit-peak-number">Peak ${index+1}</span><label>Center<input data-field="center" type="number" step="any" value="${seed.center}" required></label><label>Height<input data-field="height" type="number" min="0" step="any" value="${seed.height}" required></label><label>FWHM<input data-field="width" type="number" min="0.000001" step="any" value="${seed.width}" required></label><label class="fit-shape-field">${shapeLabel}<input data-field="shape" type="number" min="0" max="${fitMethod.value==='doniach-sunjic'?.45:1}" step="0.01" value="${seed.shape}" required></label>`;row.querySelector('.fit-shape-field').hidden=!['voigt','doniach-sunjic'].includes(fitMethod.value);fitPeakRows.append(row)}
    }
    function openFitDialog(s){
      fitTargetSpectrum=s;selectedSpectra.add(s.id);const checkbox=dataBrowser.querySelector(`.series-option input[data-spectrum-id="${s.id}"]`);if(checkbox)checkbox.checked=true;renderPlot();const energies=displayedPoints(s).map(point=>spectrumEnergy(s,point[0])).filter(Number.isFinite),fullDomain=[Math.min(...energies),Math.max(...energies)],visibleDomain=zoomDomain&&zoomDomain[1]>fullDomain[0]&&zoomDomain[0]<fullDomain[1]?[Math.max(fullDomain[0],zoomDomain[0]),Math.min(fullDomain[1],zoomDomain[1])]:fullDomain;fitSpectrumName.textContent=`${s.group} / ${s.region} / Spectrum ${s.spectrumId||'—'}`;fitEnergyMin.value=String(Number(visibleDomain[0].toFixed(6)));fitEnergyMax.value=String(Number(visibleDomain[1].toFixed(6)));fitDialogError.hidden=true;fitPeakRows.innerHTML='';updateFitMethodUI();fitDialog.showModal();fitEnergyMin.focus()
    }
    fitPeakCount.onchange=renderFitPeakRows;
    fitEnergyMin.onchange=renderFitPeakRows;fitEnergyMax.onchange=renderFitPeakRows;
    function updateFitMethodUI(){const isFermi=fitMethod.value==='fermi-edge';fitPeakCountLabel.hidden=isFermi;fitBackgroundLabel.hidden=isFermi;fitPeakSection.hidden=isFermi;fitNote.textContent=isFermi?`The Fermi-edge background is linear for ${mode==='binding'?'binding energies above EF':'kinetic energies below EF'} and constant on the opposite side.`:'Choose no background, constant, linear, or Shirley background. Initial peak values remain editable.';if(!isFermi)renderFitPeakRows()}
    fitMethod.onchange=updateFitMethodUI;
    fitForm.querySelector('[value="cancel"]').onclick=()=>fitDialog.close();
    fitForm.onsubmit=event=>{event.preventDefault();const s=fitTargetSpectrum,min=fitEnergyMin.valueAsNumber,max=fitEnergyMax.valueAsNumber;if(!s||!Number.isFinite(min)||!Number.isFinite(max)||min>=max){fitDialogError.textContent='Select one spectrum and enter a valid energy range.';fitDialogError.hidden=false;return}const points=spectrumFitPoints(s,min,max);try{let result;if(fitMethod.value==='fermi-edge')result=XPSFitting.fitFermiEdge(points,{backgroundSide:mode==='binding'?'higher':'lower',edgeDirection:mode==='binding'?'rising':'falling'});else{const peaks=[...fitPeakRows.querySelectorAll('.fit-peak-row')].map(row=>({center:Number(row.querySelector('[data-field="center"]').value),height:Number(row.querySelector('[data-field="height"]').value),width:Number(row.querySelector('[data-field="width"]').value),shape:Number(row.querySelector('[data-field="shape"]').value)}));result=XPSFitting.fitPeaks(points,{type:fitMethod.value,backgroundType:fitBackground.value,peaks})}const fitIndex=(s.fits||[]).length,saved={...result,id:`fit-${Date.now()}`,name:`${fitTypeLabel(result.type)} fit ${fitIndex+1}`,range:[min,max],color:fitColor(fitIndex),visible:true,lineStyle:'solid',lineWidth:2.5,showComponents:true,showBackground:true,showAnnotation:true,createdAt:new Date().toISOString()};s.fits=s.fits||[];s.fits.push(saved);s.fitResultsOpen=true;fitDialog.close();renderDataBrowser();renderPlot();showAnalysisMessage(`${saved.name} completed for ${s.region}. R² ${saved.rSquared.toFixed(5)}.`)}catch(error){fitDialogError.textContent=error.message;fitDialogError.hidden=false}};
    fitStyleForm.querySelector('[value="cancel"]').onclick=()=>fitStyleDialog.close();
    fitStyleForm.onsubmit=event=>{event.preventDefault();if(!fitStyleTarget)return;fitStyleTarget.color=fitLineColor.value;fitStyleTarget.lineStyle=fitLineStyle.value;fitStyleTarget.lineWidth=fitLineWidth.valueAsNumber;fitStyleTarget.showComponents=fitShowComponents.checked;fitStyleTarget.showBackground=fitShowBackground.checked;fitStyleTarget.showAnnotation=fitShowAnnotation.checked;fitStyleDialog.close();renderDataBrowser();renderPlot()};
    function displayedPoints(s){return activeAnalysis==='raw'?(s.points):(analysisSnapshots.get(activeAnalysis)?.points.get(s.id)||s.points)}
    function spectrumEnergy(s,kinetic){return mode==='kinetic'?kinetic:(Number.isFinite(s.excitation)?s.excitation:photon)-kinetic-workFunction}
    function automaticEnergyDomain(){
      let min=Infinity,max=-Infinity;
      selectedData().filter(s=>!s.groupPlotHidden&&!plotStyle(s).hidden).forEach(s=>{const style=plotStyle(s);displayedPoints(s).forEach(([kinetic])=>{const energy=spectrumEnergy(s,kinetic)+style.xOffset;if(Number.isFinite(energy)){min=Math.min(min,energy);max=Math.max(max,energy)}})});
      if(!Number.isFinite(min))return[0,photon];
      if(min===max){const pad=Math.max(Math.abs(min)*.001,.1);return[min-pad,max+pad]}
      return[min,max];
    }
    function visibleSpectra(domain){return selectedData().filter(s=>!s.groupPlotHidden&&!plotStyle(s).hidden).map(s=>{const style=plotStyle(s);return{s,style,points:displayedPoints(s).map(([kinetic,count])=>[spectrumEnergy(s,kinetic)+style.xOffset,count+style.yOffset]).filter(([energy,count])=>energy>=domain[0]&&energy<=domain[1]&&Number.isFinite(count))}}).filter(item=>item.points.length>1)}
    function showAnalysisMessage(message){clearTimeout(analysisMessageTimer);analysisMessage.textContent=message;analysisMessage.hidden=false;analysisMessageTimer=setTimeout(()=>{analysisMessage.hidden=true},4500)}
    function analysisLabel(type){return{constant:'Constant background',height:'Normalize height',area:'Normalize area'}[type]}
    function refreshAnalysisStateOptions(){
      analysisState.replaceChildren(new Option('raw (Before analysis)','raw'));
      [...analysisSnapshots].sort(([,a],[,b])=>a.stage-b.stage).forEach(([id,snapshot])=>{const option=new Option(id,id);option.title=(snapshot.steps||[]).map(analysisLabel).join(' → ');analysisState.append(option)});
      analysisState.value=activeAnalysis;
    }
    function currentPointMap(){const map=new Map();dataFiles.flatMap(file=>file.spectra).forEach(s=>map.set(s.id,displayedPoints(s)));return map}
    function pointsInEnergyRange(s,points,min,max){return points.filter(([kinetic,count])=>{const energy=spectrumEnergy(s,kinetic);return energy>=min&&energy<=max&&Number.isFinite(count)})}
    function analysisDivisor(type,rangePoints){
      if(type==='constant')return rangePoints.reduce((sum,point)=>sum+point[1],0)/rangePoints.length;
      if(type==='height')return Math.max(...rangePoints.map(point=>point[1]));
      let area=0;for(let i=1;i<rangePoints.length;i++){const previous=rangePoints[i-1],current=rangePoints[i];area+=Math.abs(current[0]-previous[0])*(previous[1]+current[1])/2}return area;
    }
    function runAnalysis(type,min,max){
      const spectra=selectedData();if(!spectra.length)throw new Error('Select at least one measurement spectrum before running an analysis.');
      const results=[],failures=[];
      spectra.forEach(s=>{const source=displayedPoints(s),rangePoints=pointsInEnergyRange(s,source,min,max),minimumPoints=type==='area'?2:1;if(rangePoints.length<minimumPoints){failures.push(s.region);return}const factor=analysisDivisor(type,rangePoints);if(type!=='constant'&&(!Number.isFinite(factor)||Math.abs(factor)<1e-15)){failures.push(s.region);return}const points=source.map(([energy,intensity])=>[energy,type==='constant'?intensity-factor:intensity/factor]);results.push([s.id,points])});
      if(failures.length)throw new Error(`The selected range cannot be analyzed for: ${failures.join(', ')}.`);
      const points=currentPointMap();results.forEach(([id,data])=>points.set(id,data));const parent=analysisSnapshots.get(activeAnalysis),steps=[...(parent?.steps||[]),type],id=activeAnalysis==='raw'?ANALYSIS_CODES[type]:`${activeAnalysis}_${ANALYSIS_CODES[type]}`,stage=Math.max(0,...[...analysisSnapshots.values()].map(snapshot=>snapshot.stage))+1;analysisSnapshots.set(id,{points,stage,min,max,type,steps});activeAnalysis=id;refreshAnalysisStateOptions();zoomYDomain=null;renderPlot();showAnalysisMessage(`${id}: ${analysisLabel(type)} completed for ${spectra.length} spectrum${spectra.length===1?'':'s'} (${min.toFixed(2)}–${max.toFixed(2)} eV).`)
    }
    function automaticIntensityDomain(spectra){let min=Infinity,max=-Infinity;spectra.forEach(item=>item.points.forEach(([,value])=>{min=Math.min(min,value);max=Math.max(max,value)}));if(!Number.isFinite(min))return null;if(min===max){const pad=Math.max(Math.abs(min)*.05,1);return[min-pad,max+pad]}const pad=(max-min)*.05;return[min-pad,max+pad]}
    function formatIntensity(value){const absolute=Math.abs(value);if(absolute>=1e6)return `${(value/1e6).toFixed(1).replace(/\.0$/,'')}M`;if(absolute>=1e3)return `${(value/1e3).toFixed(1).replace(/\.0$/,'')}k`;return value.toLocaleString(undefined,{maximumFractionDigits:1})}
    function markerNode(shape,cx,cy,style,color){const r=style.markerSize,common={fill:style.markerFilled?color:'none',stroke:color,'stroke-width':style.markerStrokeWidth,'vector-effect':'non-scaling-stroke'};if(shape==='square')return node('rect',{x:cx-r,y:cy-r,width:r*2,height:r*2,...common});if(shape==='diamond')return node('polygon',{points:`${cx},${cy-r} ${cx+r},${cy} ${cx},${cy+r} ${cx-r},${cy}`,...common});if(shape==='triangle')return node('polygon',{points:`${cx},${cy-r} ${cx+r*.9},${cy+r*.75} ${cx-r*.9},${cy+r*.75}`,...common});if(shape==='cross')return node('path',{d:`M${cx-r} ${cy}L${cx+r} ${cy}M${cx} ${cy-r}L${cx} ${cy+r}`,fill:'none',stroke:color,'stroke-width':Math.max(1,style.markerStrokeWidth),'vector-effect':'non-scaling-stroke'});return node('circle',{cx,cy,r,...common})}
    function drawSpectra(spectra,x,y){
      spectra.forEach(({s,style,points})=>{const stride=Math.max(1,Math.ceil(points.length/2400)),sampled=points.filter((point,i)=>i%stride===0||i===points.length-1),group=node('g',{'clip-path':'url(#plotClip)',opacity:.92});const title=node('title',{});title.textContent=`${s.region} · Spectrum ${s.spectrumId||'—'}`;group.append(title);
        if(style.renderMode!=='markers'){let d='';sampled.forEach(([energy,value])=>{d+=`${d?'L':'M'}${x(energy).toFixed(2)} ${y(value).toFixed(2)} `});group.append(node('path',{d,fill:'none',stroke:s.color,'stroke-width':style.lineWidth,'stroke-dasharray':style.lineStyle==='dashed'?'8 5':'none','vector-effect':'non-scaling-stroke'}))}
        if(style.renderMode!=='line')sampled.forEach(([energy,value])=>group.append(markerNode(style.markerShape,Number(x(energy).toFixed(2)),Number(y(value).toFixed(2)),style,s.color)));
        svg.append(group)
      });
    }
    function fitDash(style){return style==='dashed'?'8 5':style==='dotted'?'2 4':'none'}
    function nearestPoint(points,energy){return points.reduce((best,point)=>!best||Math.abs(point[0]-energy)<Math.abs(best[0]-energy)?point:best,null)}
    function resultParameterNumber(operation,name){return Number.parseFloat(operation.parameters?.[name])}
    function visibleFitOverlays(domain){
      const overlays=[];
      selectedData().filter(s=>!s.groupPlotHidden&&!plotStyle(s).hidden).forEach(s=>{
        const plot=plotStyle(s),transform=(points,kinetic=false)=>points.map(([energy,value])=>[(kinetic?spectrumEnergy(s,energy):energy)+plot.xOffset,value+plot.yOffset]).filter(point=>point[0]>=domain[0]&&point[0]<=domain[1]);
        (s.fits||[]).filter(fit=>fit.visible!==false).forEach(fit=>{
          normalizeFitLineStyle(fit);const totalPoints=transform(fit.points),total={name:fit.name,color:fit.color,width:fit.lineWidth,dash:fitDash(fit.lineStyle),points:totalPoints};if(fit.type==='fermi-edge'&&fit.showAnnotation){const energy=fit.parameters.center+plot.xOffset,point=nearestPoint(totalPoints,energy);if(point)total.annotation={energy,value:point[1],center:fit.parameters.center,width:fit.parameters.width}}overlays.push(total);
          if(fit.showBackground&&fit.backgroundPoints?.length)overlays.push({name:`${fit.name} · Background`,color:fit.color,width:Math.max(1,fit.lineWidth*.55),dash:'3 4',opacity:.75,points:transform(fit.backgroundPoints)});
          if(fit.showComponents){
            if(fit.componentPoints?.length)fit.componentPoints.forEach((points,index)=>overlays.push({name:`${fit.name} · Peak ${index+1}`,color:fitColor(index+1),width:Math.max(1,fit.lineWidth*.6),dash:'5 4',opacity:.8,points:transform(points)}));
            else(fit.components||[]).forEach((component,index)=>overlays.push({name:`${fit.name} · ${component.name}`,color:fitColor(index+1),width:Math.max(1,fit.lineWidth*.6),dash:'5 4',opacity:.8,points:transform(component.points)}));
          }
        });
        (s.results||[]).forEach(operation=>operation.curves.filter(curve=>curve.visible&&!curve.removed).forEach(curve=>{normalizeFitLineStyle(curve,true);const points=transform(curve.points,true),overlay={name:`${operation.name} · ${curve.name}`,color:curve.color,width:curve.lineWidth,dash:fitDash(curve.lineStyle),points};if(curve.showAnnotation&&operation.name.toLowerCase().includes('fermi')&&curve.name.toLowerCase().includes('fermi')){const rawCenter=resultParameterNumber(operation,'Center'),width=resultParameterNumber(operation,'FWHM'),energy=spectrumEnergy(s,rawCenter)+plot.xOffset,point=nearestPoint(points,energy);if(Number.isFinite(energy)&&Number.isFinite(width)&&point)overlay.annotation={energy,value:point[1],center:energy-plot.xOffset,width}}overlays.push(overlay)}));
      });
      return overlays.filter(item=>item.points.length>1)
    }
    function drawFitOverlays(overlays,x,y){overlays.forEach(overlay=>{let d='';overlay.points.forEach(([energy,value])=>{d+=`${d?'L':'M'}${x(energy).toFixed(2)} ${y(value).toFixed(2)} `});const path=node('path',{class:'fit-overlay',d,fill:'none',stroke:overlay.color,'stroke-width':overlay.width,'stroke-dasharray':overlay.dash,opacity:overlay.opacity??1,'vector-effect':'non-scaling-stroke','clip-path':'url(#plotClip)'});const title=node('title',{});title.textContent=overlay.name;path.append(title);svg.append(path);if(overlay.annotation){const xx=x(overlay.annotation.energy),yy=y(overlay.annotation.value),rightSide=xx>plotScale.W-190,tx=xx+(rightSide?-9:9),ty=yy<55?yy+18:yy-20,textNode=node('text',{class:'fit-annotation',x:tx,y:ty,fill:overlay.color,'font-size':11,'font-weight':700,'text-anchor':rightSide?'end':'start'}),first=node('tspan',{x:tx,dy:0}),second=node('tspan',{x:tx,dy:15});first.textContent=`EF = ${overlay.annotation.center.toFixed(3)} eV`;second.textContent=`FWHM = ${overlay.annotation.width.toFixed(3)} eV`;textNode.append(first,second);svg.append(textNode)}})}
    function legendEntry(s){if(!plotLegendState.entries[s.id])plotLegendState.entries[s.id]={name:`${s.region} · ${s.spectrumId||'—'}`,fontSize:12,deleted:false};return plotLegendState.entries[s.id]}
    function placePlotLegend(){if(plotLegend.hidden)return;const wrap=plotLegend.parentElement,maxX=Math.max(0,wrap.clientWidth-plotLegend.offsetWidth),maxY=Math.max(0,wrap.clientHeight-plotLegend.offsetHeight);plotLegend.style.left=`${plotLegendState.x*maxX}px`;plotLegend.style.top=`${plotLegendState.y*maxY}px`}
    function renderPlotLegend(){plotLegend.innerHTML='';selectedData().filter(s=>!s.groupPlotHidden&&!plotStyle(s).hidden).forEach(s=>{const entry=legendEntry(s);if(entry.deleted)return;const item=document.createElement('div');item.className='plot-legend-item';item.dataset.id=s.id;item.title='Double-click to edit';item.style.fontSize=`${entry.fontSize}px`;const swatch=document.createElement('i');swatch.className='plot-legend-swatch';swatch.style.setProperty('--legend-color',s.color);const label=document.createElement('span');label.textContent=entry.name;item.append(swatch,label);item.ondblclick=event=>{event.stopPropagation();openLegendEditor(s.id)};plotLegend.append(item)});plotLegend.hidden=!plotLegendState.visible||!plotLegend.children.length;plotLegendButton.textContent=plotLegendState.visible?'Hide legend':'Legend';requestAnimationFrame(placePlotLegend)}
    function renderPlot(){
      const W=900,H=340,left=82,right=12,top=14,bottom=38,domain=zoomDomain||automaticEnergyDomain(),span=domain[1]-domain[0],step=axisScale(span).step,spectra=visibleSpectra(domain),fitOverlays=visibleFitOverlays(domain),yDomain=zoomYDomain||automaticIntensityDomain([...spectra,...fitOverlays]),ySpan=yDomain?yDomain[1]-yDomain[0]:null;
      svg.setAttribute('viewBox',`0 0 ${W} ${H}`); svg.innerHTML='';
      const defs=node('defs',{}),clip=node('clipPath',{id:'plotClip'});clip.append(node('rect',{x:left,y:top,width:W-left-right,height:H-top-bottom}));defs.append(clip);svg.append(defs);
      const x=e=>left+(mode==='binding'?(domain[1]-e):(e-domain[0]))/span*(W-left-right),y=value=>top+(yDomain[1]-value)/ySpan*(H-top-bottom); plotScale={W,H,left,right,top,bottom,domain,yDomain,x,y};
      for(let e=Math.ceil(domain[0]/step)*step;e<=domain[1]+step/1000;e+=step){const value=Number(e.toPrecision(12)),xx=x(value),line=node('line',{x1:xx,y1:top,x2:xx,y2:H-bottom,stroke:'#1f3940','stroke-width':1});svg.append(line);const t=node('text',{x:xx,y:H-10,fill:'#a4b9b6','font-size':13,'font-weight':600,'text-anchor':'middle'});t.textContent=value.toLocaleString();svg.append(t)}
      if(yDomain){const yStep=axisScale(ySpan).step;for(let value=Math.ceil(yDomain[0]/yStep)*yStep;value<=yDomain[1]+yStep/1000;value+=yStep){const yy=y(value);svg.append(node('line',{x1:left,y1:yy,x2:W-right,y2:yy,stroke:'#1f3940','stroke-width':1}));const t=node('text',{x:left-10,y:yy+4,fill:'#a4b9b6','font-size':13,'font-weight':600,'text-anchor':'end'});t.textContent=formatIntensity(value);svg.append(t)}svg.append(node('line',{x1:left,y1:top,x2:left,y2:H-bottom,stroke:'#426069'}))}
      const activeAnalysisType=analysisSnapshots.get(activeAnalysis)?.type;yAxisLabel.hidden=!yDomain;yAxisLabel.textContent=!activeAnalysisType||activeAnalysisType==='constant'?'Intensity (counts/s)':'Normalized intensity';
      svg.append(node('line',{x1:left,y1:H-bottom,x2:W-right,y2:H-bottom,stroke:'#426069'}));
      if(yDomain){drawSpectra(spectra,x,y);drawFitOverlays(fitOverlays,x,y)}
      const active=[...selected].filter(([,item])=>item.visible); if(!active.length&&!spectra.length){const t=node('text',{x:W/2,y:H/2,fill:'#607d7a','font-size':13,'text-anchor':'middle'});t.textContent=selectedData().length?'Measurement plots are hidden':selected.size?'Selected elements are hidden':'Select an element or measurement data';svg.append(t)}
      let ci=0; active.forEach(([s,item])=>{ const all=[]; if(showCoreInput.checked)peaks[s].core.forEach(p=>all.push([...p,'core'])); if(showAugerInput.checked)peaks[s].auger.forEach(p=>all.push([...p,'auger'])); all.forEach(([label,raw,type],j)=>{ const e=displayedEnergy(raw,type); if(e<domain[0]||e>domain[1]||e<0||e>photon)return; const xx=x(e),dash=type==='auger'?'5 4':'0',l=node('line',{x1:xx,y1:top+(ci%12)*3,x2:xx,y2:H-bottom,stroke:type==='auger'?'#ffb45d':item.color,'stroke-width':2,'stroke-dasharray':dash,opacity:.9});l.onmouseenter=ev=>showPeakTip(ev,s,label,e,raw,type);l.onmousemove=ev=>showPeakTip(ev,s,label,e,raw,type);l.onmouseleave=()=>tip.style.display='none';svg.append(l);const t=node('text',{x:xx+4,y:21+((j+ci)%7)*17,fill:type==='auger'?'#ffb45d':item.color,'font-size':12,'font-weight':650});t.textContent=`${s} ${label}`;svg.append(t)});ci++});
      renderPlotLegend();schedulePersist();
    }
    function showPeakTip(ev,s,label,e,raw,type){tip.style.display='block';tip.style.left=ev.clientX+12+'px';tip.style.top=ev.clientY+12+'px';tip.innerHTML=`<b>${s} ${label}</b><br>${e.toFixed(1)} eV · ${mode==='binding'?'binding':'kinetic'}<br>Reference ${raw.toFixed(1)} eV · ${type}`}
    function node(tag,a){const n=document.createElementNS('http://www.w3.org/2000/svg',tag);Object.entries(a).forEach(([k,v])=>n.setAttribute(k,v));return n}
    function pointerPoint(ev){const clientPoint=svg.createSVGPoint();clientPoint.x=ev.clientX;clientPoint.y=ev.clientY;const point=clientPoint.matrixTransform(svg.getScreenCTM().inverse());return{x:Math.max(plotScale.left,Math.min(plotScale.W-plotScale.right,point.x)),y:Math.max(plotScale.top,Math.min(plotScale.H-plotScale.bottom,point.y))}}
    function spectrumNearPlotPoint(point){let best=null,bestDistance=Infinity;visibleSpectra(plotScale.domain).forEach(({s,points})=>{points.forEach(([energy,value])=>{const dx=plotScale.x(energy)-point.x;if(Math.abs(dx)>32)return;const dy=plotScale.y(value)-point.y,distance=Math.hypot(dx,dy);if(distance<bestDistance){bestDistance=distance;best=s}})});return bestDistance<=28?best:null}
    function focusSpectrumRow(s){focusedSpectrumId=s.id;s.groupOpen=true;dataPanel.open=true;renderDataBrowser();requestAnimationFrame(()=>{const entry=dataBrowser.querySelector(`[data-spectrum-entry="${s.id}"]`);if(!entry)return;const browserRect=dataBrowser.getBoundingClientRect(),entryRect=entry.getBoundingClientRect();dataBrowser.scrollTop+=entryRect.top-browserRect.top-4});showAnalysisMessage(`${s.region} selected in Measurement data.`)}
    function energyAt(px){const {left,right,W,domain}=plotScale,n=(px-left)/(W-left-right);return mode==='binding'?domain[1]-n*(domain[1]-domain[0]):domain[0]+n*(domain[1]-domain[0])}
    function intensityAt(py){const {top,bottom,H,yDomain}=plotScale;if(!yDomain)return null;const n=(py-top)/(H-top-bottom);return yDomain[1]-n*(yDomain[1]-yDomain[0])}
    function drawCursor(px,py){svg.querySelectorAll('.interaction').forEach(n=>n.remove());svg.append(node('line',{class:'interaction',x1:px,y1:plotScale.top,x2:px,y2:plotScale.H-plotScale.bottom,stroke:'#77e4c8','stroke-width':1,opacity:.7,'pointer-events':'none'}));if(plotScale.yDomain)svg.append(node('line',{class:'interaction',x1:plotScale.left,y1:py,x2:plotScale.W-plotScale.right,y2:py,stroke:'#77e4c8','stroke-width':1,opacity:.55,'pointer-events':'none'}))}
    svg.onpointermove=ev=>{if(!plotScale)return;const point=pointerPoint(ev),energy=energyAt(point.x),intensity=intensityAt(point.y),analysisType=analysisSnapshots.get(activeAnalysis)?.type,intensityUnit=!analysisType||analysisType==='constant'?' counts/s':'';cursorReadout.textContent=intensity==null?`Cursor: ${energy.toFixed(1)} eV`:`Cursor: ${energy.toFixed(1)} eV / ${formatIntensity(intensity)}${intensityUnit}`;if(offsetDrag){const style=plotStyle(offsetDrag.s);style.xOffset=offsetDrag.initialX+energy-offsetDrag.startEnergy;if(Number.isFinite(intensity)&&Number.isFinite(offsetDrag.startIntensity))style.yOffset=offsetDrag.initialY+intensity-offsetDrag.startIntensity;offsetDrag.s.plotStyle=style;renderPlot();const refreshed=pointerPoint(ev);drawCursor(refreshed.x,refreshed.y);return}drawCursor(point.x,point.y);if(dragStart){const x=Math.min(dragStart.x,point.x),y=plotScale.yDomain?Math.min(dragStart.y,point.y):plotScale.top,width=Math.abs(point.x-dragStart.x),height=plotScale.yDomain?Math.abs(point.y-dragStart.y):plotScale.H-plotScale.bottom-plotScale.top;svg.append(node('rect',{class:'interaction',x,y,width,height,fill:'#77e4c8',stroke:'#77e4c8','stroke-width':1,opacity:.18,'pointer-events':'none'}))}};
    svg.onpointerleave=()=>{if(!dragStart&&!offsetDrag){cursorReadout.textContent='Cursor: — eV';svg.querySelectorAll('.interaction').forEach(n=>n.remove())}};
    function hideDragMenu(){dragMenu.hidden=true;dragSelection=null}
    function showDragMenu(ev,selection){const wrap=dragMenu.parentElement.getBoundingClientRect();dragMenu.querySelector('[data-action="zoom-xy"]').disabled=!selection.hasX||!selection.hasY;dragMenu.querySelector('[data-action="zoom-x"]').disabled=!selection.hasX;dragMenu.querySelector('[data-action="zoom-y"]').disabled=!selection.hasY;dragMenu.querySelectorAll('[data-action="constant"],[data-action="height"],[data-action="area"]').forEach(button=>button.disabled=!selection.hasX);dragMenu.hidden=false;const width=dragMenu.offsetWidth,height=dragMenu.offsetHeight;dragMenu.style.left=`${Math.max(8,Math.min(ev.clientX-wrap.left+8,wrap.width-width-8))}px`;dragMenu.style.top=`${Math.max(8,Math.min(ev.clientY-wrap.top+8,wrap.height-height-8))}px`;dragSelection=selection;dragMenu.querySelector('button:not(:disabled)').focus()}
    svg.onpointerdown=ev=>{if(ev.button!==0||!plotScale)return;hideDragMenu();const point=pointerPoint(ev);if(ev.ctrlKey){ev.preventDefault();const spectrum=spectrumNearPlotPoint(point);if(!spectrum){showAnalysisMessage('No measurement spectrum was found near the pointer.');return}const style=plotStyle(spectrum),previousZoomDomain=zoomDomain?zoomDomain.slice():null,previousZoomYDomain=zoomYDomain?zoomYDomain.slice():null;zoomDomain=plotScale.domain.slice();zoomYDomain=plotScale.yDomain?plotScale.yDomain.slice():null;offsetDrag={pointerId:ev.pointerId,s:spectrum,startEnergy:energyAt(point.x),startIntensity:intensityAt(point.y),initialX:style.xOffset,initialY:style.yOffset,previousZoomDomain,previousZoomYDomain};svg.setPointerCapture(ev.pointerId);svg.classList.add('offset-dragging');return}if(ev.shiftKey){ev.preventDefault();const spectrum=spectrumNearPlotPoint(point);if(spectrum)focusSpectrumRow(spectrum);else showAnalysisMessage('No measurement spectrum was found near the pointer.');return}svg.setPointerCapture(ev.pointerId);dragStart={...point,energy:energyAt(point.x),intensity:intensityAt(point.y)}};
    function finishOffsetDrag(){if(!offsetDrag)return;const completed=offsetDrag,style=plotStyle(completed.s);zoomDomain=completed.previousZoomDomain;zoomYDomain=completed.previousZoomYDomain;offsetDrag=null;svg.classList.remove('offset-dragging');renderDataBrowser();renderPlot();showAnalysisMessage(`${completed.s.region}: X offset ${style.xOffset.toFixed(3)} eV, Y offset ${formatIntensity(style.yOffset)}.`)}
    svg.onpointerup=ev=>{if(offsetDrag){if(svg.hasPointerCapture(ev.pointerId))svg.releasePointerCapture(ev.pointerId);finishOffsetDrag();return}if(!dragStart)return;const start=dragStart,point=pointerPoint(ev),distanceX=Math.abs(point.x-start.x),distanceY=Math.abs(point.y-start.y),endEnergy=energyAt(point.x),endIntensity=intensityAt(point.y),hasX=distanceX>=5,hasY=Boolean(plotScale.yDomain&&distanceY>=5);dragStart=null;svg.releasePointerCapture(ev.pointerId);if(hasX||hasY)showDragMenu(ev,{energy:[Math.min(start.energy,endEnergy),Math.max(start.energy,endEnergy)],intensity:hasY?[Math.min(start.intensity,endIntensity),Math.max(start.intensity,endIntensity)]:null,hasX,hasY});else renderPlot()};
    svg.onpointercancel=()=>{dragStart=null;hideDragMenu();finishOffsetDrag();renderPlot()};
    svg.ondblclick=()=>{hideDragMenu();zoomDomain=null;zoomYDomain=null;renderPlot()};
    dragMenu.onclick=event=>{const button=event.target.closest('button[data-action]');if(!button||button.disabled||!dragSelection)return;const action=button.dataset.action,selection=dragSelection;hideDragMenu();if(action.startsWith('zoom-')){if((action==='zoom-xy'||action==='zoom-x')&&selection.hasX)zoomDomain=selection.energy;if((action==='zoom-xy'||action==='zoom-y')&&selection.hasY)zoomYDomain=selection.intensity;renderPlot();return}pendingAnalysis=action;analysisDialogTitle.textContent=analysisLabel(action);analysisEnergyMin.value=String(Number(selection.energy[0].toFixed(6)));analysisEnergyMax.value=String(Number(selection.energy[1].toFixed(6)));analysisDialogError.hidden=true;renderPlot();analysisDialog.showModal();analysisEnergyMin.focus()};
    analysisForm.onsubmit=event=>{event.preventDefault();const min=analysisEnergyMin.valueAsNumber,max=analysisEnergyMax.valueAsNumber;if(!Number.isFinite(min)||!Number.isFinite(max)||min>=max){analysisDialogError.textContent='Enter a valid range with the minimum energy below the maximum energy.';analysisDialogError.hidden=false;return}try{runAnalysis(pendingAnalysis,min,max);analysisDialog.close()}catch(error){analysisDialogError.textContent=error.message;analysisDialogError.hidden=false}};
    analysisForm.querySelector('[value="cancel"]').onclick=()=>analysisDialog.close();
    function colorInputValue(color){
      if(/^#[0-9a-f]{6}$/i.test(color||''))return color;
      const match=String(color||'').match(/^hsl\(([\d.]+)\s+([\d.]+)%\s+([\d.]+)%\)$/i);if(!match)return'#62c9f5';
      const h=Number(match[1])/360,s=Number(match[2])/100,l=Number(match[3])/100,a=s*Math.min(l,1-l),channel=n=>{const k=(n+h*12)%12;return l-a*Math.max(-1,Math.min(k-3,9-k,1))},hex=value=>Math.round(value*255).toString(16).padStart(2,'0');return`#${hex(channel(0))}${hex(channel(8))}${hex(channel(4))}`;
    }
    function spectrumById(id){return dataFiles.flatMap(file=>file.spectra).find(s=>s.id===id)}
    function updateColorPresetSelection(){const value=plotColor.value.toLowerCase();plotColorPresets.querySelectorAll('[data-color]').forEach(button=>button.classList.toggle('active',button.dataset.color.toLowerCase()===value))}
    function fillPlotStyleForm(s){const style=plotStyle(s);plotRenderMode.value=style.renderMode;plotLineStyle.value=style.lineStyle;plotLineWidth.value=String(style.lineWidth);plotColor.value=colorInputValue(s.color);plotXOffset.value=String(style.xOffset);plotYOffset.value=String(style.yOffset);plotMarkerSize.value=String(style.markerSize);plotMarkerStrokeWidth.value=String(style.markerStrokeWidth);plotMarkerFilled.checked=style.markerFilled;plotMarkerShape.value=style.markerShape;plotHidden.checked=style.hidden;updateColorPresetSelection()}
    let layoutResetDefaults=false;
    const plotStyleInputs=new Map([[plotRenderMode,'renderMode'],[plotLineStyle,'lineStyle'],[plotLineWidth,'lineWidth'],[plotColor,'color'],[plotXOffset,'xOffset'],[plotYOffset,'yOffset'],[plotMarkerSize,'markerSize'],[plotMarkerStrokeWidth,'markerStrokeWidth'],[plotMarkerFilled,'markerFilled'],[plotMarkerShape,'markerShape'],[plotHidden,'hidden']]);
    plotStyleInputs.forEach((key,input)=>input.addEventListener('input',()=>layoutDirty.add(key)));
    plotColor.addEventListener('input',updateColorPresetSelection);
    plotColorPresets.querySelectorAll('[data-color]').forEach(button=>{button.style.setProperty('--preset-color',button.dataset.color);button.onclick=()=>{plotColor.value=button.dataset.color;layoutDirty.add('color');updateColorPresetSelection()}});
    function refreshLayoutSelection(loadValues=true){const spectra=selectedData(),valid=new Set(spectra.map(s=>s.id));[...layoutSelection].forEach(id=>{if(!valid.has(id))layoutSelection.delete(id)});plotSeriesList.querySelectorAll('.plot-series-row').forEach(row=>row.classList.toggle('is-selected',layoutSelection.has(row.dataset.id)));plotSelectionCount.textContent=`${layoutSelection.size} selected`;const hasSelection=layoutSelection.size>0;plotLayoutControls.hidden=!hasSelection;plotLayoutForm.querySelector('button[type="submit"]').disabled=!hasSelection;plotStyleReset.disabled=!hasSelection;if(loadValues&&hasSelection){const first=spectra.find(s=>layoutSelection.has(s.id));fillPlotStyleForm(first);layoutDirty.clear();layoutResetDefaults=false}}
    function movePlotBefore(sourceId,targetId){normalizePlotOrder();const from=plotOrder.indexOf(sourceId),target=plotOrder.indexOf(targetId);if(from<0||target<0||from===target)return;plotOrder.splice(from,1);plotOrder.splice(plotOrder.indexOf(targetId),0,sourceId)}
    function renderPlotSeriesList(){plotSeriesList.innerHTML='';selectedData().forEach(s=>{const style=plotStyle(s),row=document.createElement('div');row.className=`plot-series-row${layoutSelection.has(s.id)?' is-selected':''}${style.hidden?' is-hidden':''}`;row.draggable=true;row.dataset.id=s.id;row.style.setProperty('--series-color',s.color);const checkbox=document.createElement('input');checkbox.type='checkbox';checkbox.checked=layoutSelection.has(s.id);checkbox.setAttribute('aria-label',`Select ${s.region}`);const handle=document.createElement('span');handle.className='plot-drag-handle';handle.textContent='⠿';handle.setAttribute('aria-hidden','true');const name=document.createElement('span');name.className='plot-series-name';name.textContent=`${s.region} · ${s.spectrumId||'—'}`;name.title=`${s.group} / ${s.region} / Spectrum ${s.spectrumId||'—'}`;checkbox.onclick=event=>event.stopPropagation();checkbox.onchange=()=>{checkbox.checked?layoutSelection.add(s.id):layoutSelection.delete(s.id);refreshLayoutSelection()};row.onclick=()=>{layoutSelection.has(s.id)?layoutSelection.delete(s.id):layoutSelection.add(s.id);checkbox.checked=layoutSelection.has(s.id);refreshLayoutSelection()};row.ondragstart=event=>{event.dataTransfer.effectAllowed='move';event.dataTransfer.setData('text/plain',s.id);row.classList.add('dragging')};row.ondragover=event=>{event.preventDefault();event.dataTransfer.dropEffect='move';row.classList.add('drag-over')};row.ondragleave=()=>row.classList.remove('drag-over');row.ondrop=event=>{event.preventDefault();row.classList.remove('drag-over');const sourceId=event.dataTransfer.getData('text/plain');movePlotBefore(sourceId,s.id);renderPlotSeriesList();renderPlot();schedulePersist()};row.ondragend=()=>plotSeriesList.querySelectorAll('.plot-series-row').forEach(item=>item.classList.remove('dragging','drag-over'));row.append(checkbox,handle,name);plotSeriesList.append(row)})}
    function openPlotLayout(preferredId,selectAll=false){const spectra=selectedData(),hasSpectra=spectra.length>0;layoutSelection.clear();if(hasSpectra){if(selectAll)spectra.forEach(s=>layoutSelection.add(s.id));else layoutSelection.add(spectra.some(s=>s.id===preferredId)?preferredId:spectra[0].id)}plotLayoutEmpty.hidden=hasSpectra;plotLayoutBody.hidden=!hasSpectra;renderPlotSeriesList();refreshLayoutSelection();plotLayoutDialog.showModal();if(hasSpectra)plotSeriesList.querySelector('input').focus()}
    plotLayoutButton.onclick=()=>openPlotLayout();
    plotSelectAll.onclick=()=>{selectedData().forEach(s=>layoutSelection.add(s.id));renderPlotSeriesList();refreshLayoutSelection()};
    plotSelectNone.onclick=()=>{layoutSelection.clear();renderPlotSeriesList();refreshLayoutSelection()};
    plotStyleReset.onclick=()=>{const first=selectedData().find(s=>layoutSelection.has(s.id));if(!first)return;const index=Number(first.id.replace('data-',''));fillPlotStyleForm({...first,color:dataColor(Number.isFinite(index)?index:0),plotStyle:defaultPlotStyle()});layoutDirty.clear();layoutResetDefaults=true};
    plotLayoutForm.onsubmit=event=>{event.preventDefault();if(!plotLayoutForm.reportValidity())return;const targets=selectedData().filter(s=>layoutSelection.has(s.id)),values={renderMode:plotRenderMode.value,lineStyle:plotLineStyle.value,lineWidth:plotLineWidth.valueAsNumber,xOffset:plotXOffset.valueAsNumber,yOffset:plotYOffset.valueAsNumber,markerSize:plotMarkerSize.valueAsNumber,markerStrokeWidth:plotMarkerStrokeWidth.valueAsNumber,markerFilled:plotMarkerFilled.checked,markerShape:plotMarkerShape.value,hidden:plotHidden.checked};targets.forEach(s=>{const style=layoutResetDefaults?defaultPlotStyle():plotStyle(s);Object.entries(values).forEach(([key,value])=>{if(layoutDirty.has(key))style[key]=value});s.plotStyle=style;if(layoutResetDefaults){const index=Number(s.id.replace('data-',''));s.color=dataColor(Number.isFinite(index)?index:0)}if(layoutDirty.has('color'))s.color=plotColor.value});zoomDomain=null;zoomYDomain=null;renderDataBrowser();renderPlot();plotLayoutDialog.close()};
    function openOffsetsDialog(){const hasSpectra=selectedData().length>0;plotOffsetsError.hidden=hasSpectra;plotOffsetsError.textContent=hasSpectra?'':'Select at least one measurement spectrum first.';plotOffsetsForm.querySelector('button[type="submit"]').disabled=!hasSpectra;if(plotLayoutDialog.open)plotLayoutDialog.close();plotOffsetsDialog.showModal();if(hasSpectra)plotXOffsetStep.focus()}
    plotOffsetsButton.onclick=openOffsetsDialog;
    plotOffsetsForm.onsubmit=event=>{event.preventDefault();const xStep=plotXOffsetStep.valueAsNumber,yStep=plotYOffsetStep.valueAsNumber,targets=selectedData();if(!Number.isFinite(xStep)||!Number.isFinite(yStep)||!targets.length){plotOffsetsError.textContent='Enter valid X and Y intervals.';plotOffsetsError.hidden=false;return}targets.forEach((s,index)=>{const style=plotStyle(s);style.xOffset=index*xStep;style.yOffset=index*yStep;s.plotStyle=style});zoomDomain=null;zoomYDomain=null;renderPlot();plotOffsetsDialog.close()};
    plotOffsetsForm.querySelector('[value="cancel"]').onclick=()=>plotOffsetsDialog.close();
    function togglePlotLegend(){plotLegendState.visible=!plotLegendState.visible;renderPlotLegend();schedulePersist()}
    plotLegendButton.onclick=togglePlotLegend;
    plotLegend.onpointerdown=event=>{if(event.button!==0)return;legendDrag={pointerId:event.pointerId,startX:event.clientX,startY:event.clientY,left:plotLegend.offsetLeft,top:plotLegend.offsetTop,moved:false};plotLegend.setPointerCapture(event.pointerId);plotLegend.classList.add('dragging')};
    plotLegend.onpointermove=event=>{if(!legendDrag||event.pointerId!==legendDrag.pointerId)return;const dx=event.clientX-legendDrag.startX,dy=event.clientY-legendDrag.startY;if(Math.abs(dx)+Math.abs(dy)>3)legendDrag.moved=true;const wrap=plotLegend.parentElement,maxX=Math.max(1,wrap.clientWidth-plotLegend.offsetWidth),maxY=Math.max(1,wrap.clientHeight-plotLegend.offsetHeight);plotLegendState.x=Math.min(1,Math.max(0,(legendDrag.left+dx)/maxX));plotLegendState.y=Math.min(1,Math.max(0,(legendDrag.top+dy)/maxY));placePlotLegend()};
    plotLegend.onpointerup=event=>{if(!legendDrag||event.pointerId!==legendDrag.pointerId)return;plotLegend.releasePointerCapture(event.pointerId);plotLegend.classList.remove('dragging');legendDrag=null;schedulePersist()};
    plotLegend.onpointercancel=()=>{legendDrag=null;plotLegend.classList.remove('dragging')};
    function openLegendEditor(id){const s=spectrumById(id);if(!s)return;const entry=legendEntry(s);editingLegendId=id;legendItemName.value=entry.name;legendItemFontSize.value=String(entry.fontSize);legendEditDialog.showModal();legendItemName.focus();legendItemName.select()}
    legendEditForm.onsubmit=event=>{event.preventDefault();const entry=plotLegendState.entries[editingLegendId],fontSize=legendItemFontSize.valueAsNumber;if(!entry||!legendItemName.value.trim()||!Number.isFinite(fontSize))return;entry.name=legendItemName.value.trim();entry.fontSize=Math.min(48,Math.max(8,fontSize));entry.deleted=false;legendEditDialog.close();renderPlotLegend();schedulePersist()};
    deleteLegendItem.onclick=()=>{const entry=plotLegendState.entries[editingLegendId];if(entry)entry.deleted=true;legendEditDialog.close();renderPlotLegend();schedulePersist()};
    legendEditForm.querySelector('[value="cancel"]').onclick=()=>legendEditDialog.close();
    document.addEventListener('keydown',event=>{if(!event.ctrlKey||event.altKey||event.metaKey||event.repeat)return;if(event.key==='3'){event.preventDefault();togglePlotLegend()}else if(event.key==='4'){event.preventDefault();if(!plotOffsetsDialog.open)openOffsetsDialog()}});
    plotLayoutForm.querySelector('[value="cancel"]').onclick=()=>plotLayoutDialog.close();
    analysisState.onchange=()=>{const requested=analysisState.value;if(requested!=='raw'&&!analysisSnapshots.has(requested)){analysisState.value=activeAnalysis;showAnalysisMessage(`${requested} data does not exist yet.`);return}activeAnalysis=requested;zoomYDomain=null;renderPlot()};
    document.addEventListener('pointerdown',event=>{if(!dragMenu.hidden&&!dragMenu.contains(event.target)&&event.target!==svg){hideDragMenu();renderPlot()} });
    bindingButton.onclick=()=>setMode('binding'); kineticButton.onclick=()=>setMode('kinetic'); function setMode(m){mode=m;zoomDomain=null;zoomYDomain=null;bindingButton.classList.toggle('active',m==='binding');kineticButton.classList.toggle('active',m==='kinetic');axisLabelElement.textContent=m==='binding'?'Binding energy (eV) — high to low':'Kinetic energy (eV) — low to high';updateSearchSliderBounds();renderEnergyDetails();renderPlot()}
    selectedEnergyMode.onclick=()=>{energyTableMode='selected';renderEnergyDetails();schedulePersist()};searchEnergyMode.onclick=()=>{energyTableMode='search';renderEnergyDetails();peakEnergyInput.focus();schedulePersist()};
    firstOrderOnlyButton.onclick=()=>setHigherOrderExcitation(false);includeHigherOrdersButton.onclick=()=>setHigherOrderExcitation(true);
    function setHigherOrderExcitation(include){includeHigherOrderExcitation=include;firstOrderOnlyButton.classList.toggle('active',!include);includeHigherOrdersButton.classList.toggle('active',include);updateSearchSliderBounds();renderEnergyDetails();schedulePersist()}
    function syncSearchControl(numberInput,rangeInput,baseMax){
      const value=numberInput.valueAsNumber;
      if(Number.isFinite(value)&&value>=0){rangeInput.max=String(Math.max(baseMax,Math.ceil(value/100)*100));rangeInput.value=String(value)}
    }
    function normalizedSearchWidth(){
      const value=energyToleranceInput.valueAsNumber;
      if(!Number.isFinite(value))return NaN;
      const clamped=Math.min(SEARCH_WIDTH_MAX,Math.max(0,value));
      energyToleranceInput.value=clamped.toFixed(1);
      energyToleranceRange.value=String(clamped);
      return clamped;
    }
    function updateSearchSliderBounds(){
      syncSearchControl(peakEnergyInput,peakEnergyRange,mode==='kinetic'&&includeHigherOrderExcitation?photon*3:photon);
      energyToleranceRange.max=String(SEARCH_WIDTH_MAX);
      normalizedSearchWidth();
    }
    function bindSearchControl(numberInput,rangeInput,baseMax){
      numberInput.oninput=()=>{syncSearchControl(numberInput,rangeInput,baseMax());renderEnergyDetails();schedulePersist()};
      rangeInput.oninput=()=>{numberInput.value=Number(rangeInput.value).toFixed(1);renderEnergyDetails();schedulePersist()};
    }
    bindSearchControl(peakEnergyInput,peakEnergyRange,()=>mode==='kinetic'&&includeHigherOrderExcitation?photon*3:photon);
    energyToleranceInput.oninput=()=>{normalizedSearchWidth();renderEnergyDetails();schedulePersist()};
    energyToleranceRange.oninput=()=>{energyToleranceInput.value=Number(energyToleranceRange.value).toFixed(1);renderEnergyDetails();schedulePersist()};
    photonPreset.onchange=()=>{ if(photonPreset.value==='custom'){photonEnergy.focus();return} photonEnergy.value=photonPreset.value; updatePhoton(); };
    photonEnergy.oninput=()=>{ const exact=[...photonPreset.options].find(option=>option.value!=='custom'&&Number(option.value)===Number(photonEnergy.value)); photonPreset.value=exact?exact.value:'custom'; updatePhoton(); };
    function updatePhoton(){ const value=Number(photonEnergy.value); if(!Number.isFinite(value)||value<=0)return; photon=value;zoomDomain=null;zoomYDomain=null;updateSearchSliderBounds();renderEnergyDetails();renderPlot(); }
    workFunctionInput.oninput=()=>{const value=workFunctionInput.valueAsNumber;if(!Number.isFinite(value)||value<0)return;workFunction=value;zoomDomain=null;zoomYDomain=null;renderEnergyDetails();renderPlot()};
    showCoreInput.onchange=showAugerInput.onchange=()=>{renderEnergyDetails();renderPlot()}; clearButton.onclick=()=>{selected.clear();document.querySelectorAll('.element.selected').forEach(x=>x.classList.remove('selected'));render()};
    fileButton.onclick=event=>event.stopPropagation();
    xyFiles.onchange=async()=>{for(const file of xyFiles.files){try{addDataFile(file.name,await file.text())}catch(error){const message=document.createElement('div');message.className='data-error';message.textContent=`${file.name}: ${error.message}`;dataBrowser.prepend(message)}}xyFiles.value=''};
    energyDetails.ontoggle=dataPanel.ontoggle=schedulePersist;
    resetAllButton.onclick=async()=>{storageReady=false;clearTimeout(saveTimer);if(storageDatabase){storageDatabase.close();storageDatabase=null}await new Promise(resolve=>{const request=indexedDB.deleteDatabase(STORAGE_DB);request.onsuccess=request.onerror=request.onblocked=()=>resolve()});location.reload()};
    window.addEventListener('pagehide',()=>{clearTimeout(saveTimer);persistWorkspace()});
    async function initialize(){
      let restored=false;
      try{restored=restoreWorkspace(await readStoredWorkspace())}catch(error){console.error('Could not restore workspace state',error)}
      if(!restored){
        ['C','O','Fe'].forEach(s=>elementButton(s).click());
      }
      refreshAnalysisStateOptions();updateSearchSliderBounds();renderDataBrowser();renderFitResults();render();
      storageReady=true;schedulePersist();
    }
    initialize();
